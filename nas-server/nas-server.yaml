---
- name: nas-server playbook
  hosts: all
  roles:
    - ../roles/common
    - ../roles/docker-node
      # - ../roles/zfs

  vars:
    rw_hosts:
      - host1.example.com

  tasks:
    - name: dir-layout
      include: dir-layout.yaml
    - name: nfs-server
      include: nfs-server.yaml
    - name: samba-server
      include: samba-server.yaml
