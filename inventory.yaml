---
docker:
  hosts:
    beanmachine.localdomain:
    bigusdiskus.localdomain:
    pi-b.localdomain:

nas:
  hosts:
    bigusdiskus.localdomain:
  vars:
    rw_hosts:
      - beanmachine.localdomain
    backup_hosts:
      - beanmachine.localdomain
