daemon=5m
timeout=10

use=web, web=https://freedns.afraid.org/dynamic/check.php

syslog=no # log update msgs to syslog
pid=/var/run/ddclient.pid # record PID in file.
ssl=yes

server=freedns.afraid.org
protocol=freedns

# Login details, username in lowercase only!
login=CHANGEME
password=CHANGEME

# Domain:
CHANGEME
